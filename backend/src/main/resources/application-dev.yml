spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/medev
    username: test_user
    password: pass1
  data:
    redis:
      host: localhost
      port: 6379
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID:placeholder_client_id}
            client-secret: ${GITHUB_CLIENT_SECRET:placeholder_client_secret}
            scope: read:user,user:email
          google:
            client-id: ${GOOGLE_CLIENT_ID:placeholder_google_client_id}
            client-secret: ${GOOGLE_CLIENT_SECRET:placeholder_google_client_secret}
            scope: openid,profile,email
            redirect-uri: "{baseUrl}/login/oauth2/code/google"

jwt:
  secret: super_secret_jwt_key_that_is_at_least_256_bits_long_for_hs256_algorithm_12345

encryption:
  secret: ${ENCRYPTION_SECRET:super_secret_encryption_key_that_is_at_least_32_bytes_long_12345}

stripe:
  secret-key: sk_test_123
  webhook-secret: whsec_123

groq:
  api-key: ${GROQ_API_KEY:placeholder_groq_api_key}

logging:
  level:
    org.springframework.web.cors: INFO
    org.springframework.security: INFO
