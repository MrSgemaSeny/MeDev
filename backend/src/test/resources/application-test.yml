spring:
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: test_secret_key_that_is_long_enough_for_hs256_algorithm_for_tests
  expiration: 3600000
  refresh-expiration: 86400000

groq:
  api:
    key: test-key-123

stripe:
  api:
    key: test-key
  webhook:
    secret: test-secret

cors:
  allowed-origins: http://localhost:5173
